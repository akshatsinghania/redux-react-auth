//import { combineReducers } from 'redux';

//reducers

//theres only on reducer for now so we just export default that
import authReducer from './auth/auth';

// export default combineReducers({
// 	auth: authReducer,
// });
export default authReducer;
