import styled from 'styled-components';

const Style = styled.div`
	.navbar {
		display: flex;
		width: 50%;
		margin: auto;
	}
	.navbar * {
		margin: 1%;
	}
	.navbar__authState {
		align-self: right;
	}
`;

export default Style;
